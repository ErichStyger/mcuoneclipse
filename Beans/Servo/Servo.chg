%ifndef Shell
  %set ParseCommand Selection never
%else
  %set ParseCommand Selection always
%endif
%ifndef Trigger
  %set MovePos Selection never
  %set IsMoving Selection never
%else
  %set MovePos Selection always
  %set IsMoving Selection always
%endif
