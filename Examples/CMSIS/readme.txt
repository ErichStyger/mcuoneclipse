This folder contains a project which
- builds the CMSIS library with CodeWarrior
- and an application project which uses it

CMSIS: contains the original sources from ARM Inc.
CMSIS_Library: Library project which builds the CMSIS library
CMSIS_kl25z_app: demo application which uses the library